#include "./Huffman.h"

string Huffman::encode(string content, Map dictionary){
    cout << dictionary.find("a")->second << endl;
    
    // Todo: finish symbol map
    unordered_map<string, string> symbol_map;
    symbol_map.insert(make_pair("symbol", "binary sequence"));
    return "fff";
}

string Huffman::decode(string binary_file){
    return "fff";
}